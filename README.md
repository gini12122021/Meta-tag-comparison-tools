# Meta-tag-comparison-tools
Upload a CSV with staging and live URLs, compare HTTP status plus title, description, and keywords, then export the full report as a result CSV. Empty values on the live page are marked as ignored so they do not fail the comparison.
